# Alembic configuration. Revisions are hand-written (see migrations/env.py).
#
# Phase 6 deploy note: the API container must COPY this file plus the
# migrations/ directory so it can run "alembic upgrade head" before serving.

[alembic]
script_location = %(here)s/migrations

# Left empty on purpose: the URL comes from the SHADE_DATABASE_URL
# environment variable (resolved in migrations/env.py) or is injected
# programmatically by the test fixtures.
sqlalchemy.url =
